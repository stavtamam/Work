import { CloseWindowDirective } from './close-window.directive';

describe('CloseWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseWindowDirective();
    expect(directive).toBeTruthy();
  });
});
